<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="marla.bnv_with_viewpager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/gerd"
        android:label="GERD"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreen"
            android:theme="@style/AppTheme1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="marla.onboardingdemo.OnBoardingActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Hasil" />
        <activity android:name=".IsiArtikel" />
        <activity android:name=".GejalaGerd" />
        <activity android:name=".History"></activity>
    </application>

</manifest>